#!/usr/bin/env bash
# Pre-commit hook: run the full build check
#
# This runs lint + tests via `make check`.
# To skip (emergency only): git commit --no-verify

echo "Running pre-commit checks..."
make check
